using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ProceduralEnemyGeneration : MonoBehaviour
{
    //this needs a great pacing if we want a balanced and interesting game
    
    //creates a batch of enemy for each room, depending on the pacing wanted, then sends the info to the corresponding rooms
    void EnemyGeneration()
    {
        
    }
}
